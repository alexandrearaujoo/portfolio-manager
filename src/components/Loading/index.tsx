import { Container, Loader, Span } from './styles';

const Loading = () => {
  return (
    <Container>
      <Loader>
      <Span>Loading...</Span>
      </Loader>
    </Container>
  );
};

export default Loading;
